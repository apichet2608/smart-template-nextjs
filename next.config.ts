import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: "/smart-template",
  assetPrefix: "/smart-template/",
  output: "export",
  distDir: "dist",
  /* other config options here */
};

export default nextConfig;
